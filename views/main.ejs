<%- include('./includes/begin.ejs') %> 

    <h1>Main page</h1>

    <% if(users.length > 0) { %>
        <ul>
            <% for (let user of users) {%>
                <li><%= user.name %> </li>
            <% } %> 
        </ul>
    <% } else { %>
        <h1>You should add user</h1>
    <% } %> 

    <a href="/user">Add user</a>



<%- include('./includes/end.ejs') %> 